# Python virtual environment
venv/

# Python bytecode cache
__pycache__/
*.pyc

# Cấu hình riêng từng máy — không phải source code, KHÔNG commit
# (local_db_config.json chứa mật khẩu Postgres). Gộp chung 1 thư mục con
# config/ cạnh .exe (không phải rải rác theo cấu trúc thư mục source) — xem
# app_paths.py:get_config_dir(). File cũ ở vị trí rải rác trước đây được tự
# động di chuyển vào config/ lúc khởi động — xem main.py.
config/

# Legacy .NET SDK approach
legacy_dotnet_sdk_approach/

# Output PyInstaller — tự sinh lại từ LocalReaderMonitor.spec (local hoặc CI),
# không commit — xem docs/deploy.md
dist/
build/
app_error.log
app.lock
log/

# Editor / OS
.vscode/
Thumbs.db
.DS_Store

# Test
test_exe/
